using System;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    private void Start()
    {
        // Initialise the first screen (probably will change in the future with a menu or something)

    }
}
